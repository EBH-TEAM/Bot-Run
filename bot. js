const axios = require('axios');

const BOT_TOKEN = '8250221796:AAHdKid6RAoIeEnjHhrYdHNGkmc38wfs5d4';

async function run() {
    console.log('🤖 বট চালু হচ্ছে...');
    
    try {
        // বট চেক করা
        const me = await axios.get(`https://api.telegram.org/bot${BOT_TOKEN}/getMe`);
        console.log('✅ বট অনলাইন:', me.data.result.username);
        
        // নতুন মেসেজ চেক করা
        const updates = await axios.get(`https://api.telegram.org/bot${BOT_TOKEN}/getUpdates`);
        
        if (updates.data.result && updates.data.result.length > 0) {
            for (const update of updates.data.result) {
                if (update.message && update.message.text === '/start') {
                    const chatId = update.message.chat.id;
                    
                    const welcomeMsg = `🎉 বট চালু করার জন্য ধন্যবাদ! 🎉

✨ EBH TEAM - Habib ✨

📌 কিভাবে কাজ করবে:
1️⃣ ওয়েবসাইটে একাউন্ট সাবমিট করুন
2️⃣ সাবমিট করার পর কনফার্মেশন পাবেন
3️⃣ অ্যাডমিন অ্যাপ্রুভ করলে পেমেন্ট পাবেন

📱 সাপোর্ট:
👤 @EBH_5
📢 t.me/earningbyhabib

━━━━━━━━━━━━━━━━━━━━
EBH TEAM - Habib ❤️`;
                    
                    await axios.post(`https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`, {
                        chat_id: chatId,
                        text: welcomeMsg,
                        parse_mode: 'HTML'
                    });
                    
                    console.log('✅ ওয়েলকাম মেসেজ পাঠানো হয়েছে');
                }
            }
        }
        
        console.log('✅ কাজ শেষ');
        
    } catch (error) {
        console.log('❌ সমস্যা:', error.message);
    }
}

run();
