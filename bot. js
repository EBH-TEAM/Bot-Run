const axios = require('axios');

const BOT_TOKEN = '8250221796:AAHdKid6RAoIeEnjHhrYdHNGkmc38wfs5d4';

async function testBot() {
    console.log('🚀 বট শুরু হচ্ছে...');
    
    try {
        // টেস্ট: বটের তথ্য পাওয়া যাচ্ছে কিনা
        const info = await axios.get(`https://api.telegram.org/bot${BOT_TOKEN}/getMe`);
        console.log('✅ বটের নাম:', info.data.result.first_name);
        console.log('✅ বটের ইউজারনাম:', info.data.result.username);
        
        // টেস্ট: নিজেকে একটি টেস্ট মেসেজ পাঠান (আপনার chatId দিন)
        // আপনার chatId পেতে: https://api.telegram.org/bot8250221796:AAHdKid6RAoIeEnjHhrYdHNGkmc38wfs5d4/getUpdates
        // এখানে আপনার chatId বসিয়ে দিলে টেস্ট মেসেজ পাবেন
        
        console.log('✅ সবকিছু ঠিক আছে!');
        
    } catch (error) {
        console.error('❌ সমস্যা:', error.message);
    }
}

testBot();
